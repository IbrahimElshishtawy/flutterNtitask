import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryBlue = Color(0xFF007BFF);
  static const Color lightBlue = Color(0xFF58AFFF);

  // BMI Indicator Colors
  static const Color underweight = Color(0xFFFFD54F); // أصفر
  static const Color normal = Color(0xFF66BB6A); // أخضر
  static const Color overweight = Color(0xFFFFA726); // برتقالي
  static const Color obese = Color(0xFFE57373); // أحمر

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF757575);

  // Background Colors
  static const Color backgroundLight = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF121212);
}
